import * as React from 'react';
import Button from '@mui/material/Button';
import './tilbudbutton.css';

const tilbudbutton = () => {
    return (
        <Button variant="contained" className='tilbud_button'>Få et tilbud</Button>
    );
};

export default tilbudbutton;